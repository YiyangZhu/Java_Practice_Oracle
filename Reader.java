//the access modifier is public, so this interface can be implemented by code in any package
//if access modifier not used, the interface is available only to other members of its package
public interface Reader{
    String read();
}